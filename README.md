# Baseline — BOM Compare

Client-side BOM diff MVP. See `CLAUDE.md` for the spec.

```sh
npm install
npm run dev       # local dev server
npm test          # unit tests
npm run coverage  # tests + domain coverage threshold
npm run build     # static build in dist/
```
